# Local Docker (docker compose up)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/all_our_ideas

# Production (Neon, Supabase, etc.)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
